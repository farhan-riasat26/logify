# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A testing ground for comparing two Python logging libraries:
- **picologging** — high-performance drop-in replacement for the standard `logging` module
- **loguru** — third-party logging library with a simpler API

## Development

- **Python version:** 3.12 (see `.python-version`)
- **Package manager:** Project uses `pyproject.toml` (no dependencies yet)
- **Run:** `python main.py`
